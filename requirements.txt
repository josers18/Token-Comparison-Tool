# Heroku's Python buildpack requires a lockfile-style manifest. This file
# mirrors the `dependencies` block in pyproject.toml. Pin floors loosely;
# Heroku will resolve to the latest compatible release on each build.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pyyaml>=6.0
httpx>=0.27
python-multipart>=0.0.20
anthropic>=0.40
asyncpg>=0.30
itsdangerous>=2.2
